import React, { useMemo } from 'react';
import { BarChart, Bar, XAxis, Tooltip, ResponsiveContainer } from 'recharts';
import { Task } from '../../types';

const WeeklyProgress: React.FC<{ tasks: Task[] }

export default WeeklyProgress;